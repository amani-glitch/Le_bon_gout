"""Botler concierge — Gemini-powered chat + voice bot.

The same toolset (``tools.py``) backs both the text chat (``agent.py``) and the
real-time voice call (``live.py``), so the bot performs exactly the operations a
user could via the UI, with the same server-side pricing and validation.
"""
